name: Deploy to DockerHub

on:
 
  push:
    branches: [ ci_cd ]
# dckr_pat_c0O7ZhW7hDbioHVvLq_bZdVxzG4
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # - name: Checkout
      #   uses: actions/checkout@v3
      # - name: secret
      #   run: |
      #     echo ${{ secrets.DOCKER_HUB_USERNAME }}
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_HUB_USERNAME }}
          password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
      # - name: Set up Docker Buildx
      #   uses: docker/setup-buildx-action@v2
      # - name: Build and push
      #   uses: docker/build-push-action@v3
      #   with:
      #     context: src/api/.
      #     file: ./Dockerfile
      #     push: true
      #     tags: ${{ secrets.DOCKER_HUB_USERNAME }}/dis_sim:api_latest
  # build:
    
  #   name: Build Image
  #   runs-on: ubuntu-latest

   
  #   steps:

  #   - name: Check out code
  #     uses: actions/checkout@v2
    
  #   - name: Configure AWS credentials
  #     uses: aws-actions/configure-aws-credentials@v1
  #     with:
  #       aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
  #       aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  #       aws-region: ap-south-1

  #   - name: Login to Amazon ECR
  #     id: login-ecr
  #     uses: aws-actions/amazon-ecr-login@v1

  #   - name: Build, tag, and push image to Amazon ECR
  #     env:
  #       ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
  #       ECR_REPOSITORY: sandbox-exaindia-hackathon-jacobian # to be changes 
  #       IMAGE_TAG: ${{ github.sha }}
  #     run: |
  #       docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:1 ./Jacobian/src/api/
  #       echo "Pushing image to ECR..."
  #       docker push $ECR_REGISTRY/$ECR_REPOSITORY:1
  #       echo "::set-output name=image::$ECR_REGISTRY/$ECR_REPOSITORY:1"